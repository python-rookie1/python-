#虚拟环境(Python 解释器 和 库文件 (.py, .dll, .exe))
#cmd ：python -m venv venv (创建venv文件夹)
#cmd: venv\Scripts\activate.bat (激活虚拟环境)
#cmd ：venv\Scripts\activate.bat (安装需要的库,pip list查看)

#.env  (配置信息 (API Key, 密码, 数据库地址))
from dotenv import load_dotenv
#这行代码的作用是：打开 .env 文件，把里面的内容加载到系统的“环境变量”里
load_dotenv()

r与f的用处
f = Format (格式化)：让字符串可以嵌入变量，像填空一样。
r = Raw (原始)：让字符串里的反斜杠 \ 变成普通字符，不进行转义
